PR Bundle v4.9 — Charlotte Quick Patch
1) Copy all files in this folder into your repo root (same folder as your .git).
2) Double-click apply_pr_bundle.bat  (adds .nojekyll and injects import maps where missing).
3) Double-click git_add_commit_push.bat  (commits + pushes).
4) Visit your GitHub Pages URL and hard-refresh.

Optional: If you don't have Wings Lab in your repo yet, copy wings_lab.html into /pages/ and link it from your site.
